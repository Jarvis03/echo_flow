{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "capture-window",
  "description": "Minimal permissions for the local capture window",
  "windows": ["capture"],
  "permissions": [
    "core:default",
    "core:window:allow-hide",
    "core:window:allow-start-dragging"
  ]
}

